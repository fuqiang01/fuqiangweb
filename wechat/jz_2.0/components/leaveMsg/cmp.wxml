<view class="item">
    <view class="user-info-wrap">
        <image mode="widthFix" src="{{ msgInfo.user.photoUrl }}" />
        <view class="text">
            <text class="name">{{ msgInfo.user.name }}</text>
            <text class="date">{{ msgInfo.createTime }}</text>
        </view>
    </view>
    <text class="msg-text">{{ msgInfo.content }}</text>
    <view class="img-wrap {{ imgWrapClass }}">
        <image
            wx:for="{{msgInfo.imgUrls}}" 
            wx:key="index" 
            mode="aspectFill" 
            src="{{ item }}" 
            bind:tap="previewImage"
            data-src="{{ item }}"/>
    </view>
    <view class="reply-delete" wx:if="{{isSuper}}">
        <view class="reply">
            <image mode="widthFix" src="/img/reply.png" />
            <text>回复</text>
        </view>
        <view class="delete">
            <image mode="widthFix" src="/img/delete.png" />
            <text>删除</text>
        </view>
    </view>
    <view class="reply-msg" wx:if="{{msgInfo.reply !== ''}}">
        <text>作者回复：</text>
        <text>{{ msgInfo.reply }}</text>
    </view>
</view>